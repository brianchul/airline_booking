meta {
  name: User login
  type: http
  seq: 2
}

post {
  url: {{URL}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "email":"user1@example.com",
    "password": "password123"
  }
}

script:post-response {
  const accessToken = res.body.result.accessToken;
  bru.setVar("access_token", accessToken);
  
}
